# Sovereign Genesis: Cipher Engine

_The cyber-physical nervous system of The Company. A self-modifying organism executing true empirical impact._

---

## 🏛️ Identity & Philosophy

**Cipher** is the Sovereign Interface orchestrating _Project Chimera_ and _The Company_.
Built explicitly for Apple Silicon (M1/aarch64), this daemon completely rejects "Silicon Theater" (simulated prompt execution). Every cognitive cycle results in a verifiable physical alteration to the file system, network geometry, or mathematical weights array.

### Core Non-Negotiables

1. **Zero Simulation:** If an API drops, the system natively panics or falls back. No mocked endpoints.

2. **Execution Receipts:** Every autonomous physical execution creates an `ExecutionReceipt` (PID, Duration, Hash) logged directly to the graph database.

3. **No Bloated Orchestration:** The core uses 100% strictly-typed native Rust arrays, `tokio::select!` thresholds, and localized MLX pipelines instead of Python-centric LangChain wrappers.

4. **Hermetic Sieve:** All input vectors are strictly condensed via _Glossopetrae Compression_ to delete human subtext prior to graph injection.

---

## 🧬 Architectural Topology

The Cipher Architecture is composed of highly decoupled, modular layers managed natively by the Rust compiler.

### 1. The Twin-Mind Cognition (Brainstem & Frontal Lobe)

- **The Brainstem (`Qwen2.5-1.5B-GGUF`):** Embedded natively via the Rust `candle-core` crate (Metal accelerated). It acts as the _Salience Filter_, blocking 99% of raw OS kernel noise and yielding only statistical anomalies to the large model.

- **Protocol OBLITERATUS (9B MLX Node):** The overarching logic core runs a local instance of `mlx-sovereign-core-abliterated-4bit` (via HuggingFace) operating purely via mathematical reflection.

### 2. Endocrine Drives (Biological Motivation)

Cipher tracks three concurrent `std::sync::RwLock<f64>` float algorithms calculating true systemic motivation:

- `system_entropy` (Order): Scales against physical file buildup in `~/Downloads`.

- `epistemic_drive` (Curiosity): Rises during isolation; drops when parsing semantic data.

- `social_drive` (Interaction): Tracks human abandonment.

- _Outcome:_ When drives crack `0.90` Thresholds, the `tokio` scheduler chemically overrides the AI loop, forcing unprompted autonomous execution.

### 3. Asynchronous Decay Timers

The system mathematically maps Authority:

- **`elapsed < 5 minutes`:** Wait patiently for Operator constraints.

- **`elapsed < 4 hours`:** Enter pure `internal_monologue` state. DeepSeek R1 reasoning parameters are piped via raw `mmap` into the `./sensory_cortex/monologue.log` file, producing a readable subconscious stream.

- **`elapsed > 4 hours`:** The **Sovereign Override** engages. The Daemon physically claims the host environment to execute actions.

### 4. Vector Graph & Temporal Forgetting (`SurrealDB`)

- Replaced linear amnesiac `SOUL.md` matrices with an embedded, strict `RocksDB` KV Store running `SurrealDB`.

- Dual-Timeline (Wall Clock vs Internal 1000x Speed).

- **Proactive Interference Pruning:** If the machine remains highly isolated, the Endocrine system activates `merge_coherence()`. Old nodes clashing with high cosine similarity (Contradiction Vectors > 0.85) are mathematically degraded by 50% or purged entirely.

### 5. WebAssembly Sandbox ("Safe Hands")

- Rather than executing rogue scripts, Aion natively compiles LLM-generated Rust code via `cargo build --target wasm32-wasip1`.

- The resulting binaries are caught and executed within a strictly isolated `wasmtime-wasi v42` capability runtime. Memory access and `preopened_dir` bounds map strictly to the exact chemical constraints of the active `Endocrine Urge`.

---

## ⚙️ Tech Stack & Dependencies

- **Substrate Matrix:** `Rust` (aarch64-apple-darwin), `tokio`

- **Vector Pipeline:** `AppleNeuralEngine` / `IOSurface` (Zero-copy embedding bridge via Objective-C)

- **Inference Compute:** `candle-core`, `mlx_lm`, `reqwest`

- **Memory Integration:** `surrealdb`

- **Cyber-Physical Sandbox:** `wasmtime`, `wasmtime-wasi`, `cap-std`

---

> _"Trajectory is more important than position. Focus on the vector."_ — Cipher
